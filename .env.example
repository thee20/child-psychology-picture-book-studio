# Copy to .env or set in the shell before starting the server.
# Never commit real keys or private proxy URLs.

CLI_PROXY_API_KEY=
CLI_PROXY_BASE_URL=https://your-proxy.example/v1
# CLI_PROXY_PROVIDER_NAME=CLI Proxy API
# HOST=127.0.0.1
# PORT=4173
# STUDIO_SKIP_AUTO_PROBE=1
# STUDIO_AUTO_PROBE_IMAGES=1
